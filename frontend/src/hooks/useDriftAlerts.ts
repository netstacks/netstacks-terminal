/**
 * useDriftAlerts - placeholder for future drift detection
 *
 * Drift detection will be implemented in a future phase of the
 * config management engine. This hook is a no-op stub.
 */

export function useDriftAlerts(): { isListening: boolean } {
  return { isListening: false };
}
